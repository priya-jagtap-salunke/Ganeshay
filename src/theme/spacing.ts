/**
 * Material Design 3 spacing (4dp grid) and shape scale.
 * Touch targets should be at least 48dp.
 */
export const spacing = {
  none: 0,
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  xxxl: 64,
};

/** MD3 shape tokens (corner radius). */
export const radius = {
  none: 0,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 28,
  full: 999,
};

/** Minimum interactive size (MD accessibility). */
export const touchTarget = {
  min: 48,
  comfortable: 56,
};
